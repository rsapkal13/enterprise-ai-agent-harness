# Skill Schema

The skill schema defines a governed business capability.

See `schemas/skill.schema.json` for the draft JSON Schema. The current version is intentionally small and will expand as the registry model matures.
