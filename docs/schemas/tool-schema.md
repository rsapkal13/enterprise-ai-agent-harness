# Tool Schema

The tool schema defines a governed wrapper around an action or lookup, including target system, side-effect profile, risk, and audit expectations.

See `schemas/tool.schema.json` for the draft JSON Schema.
