# Workflow Schema

The workflow schema defines stateful business journeys, including ordered steps, rollback guidance, and evaluation references.

See `schemas/workflow.schema.json` for the draft JSON Schema.
