# Versioning

Every registry object should include a version once the schemas move beyond placeholders.

Versioning should support:

- Schema compatibility checks
- Safe evolution of skills and workflows
- Deprecation of tools and policies
- Reproducible audit records
- Example validation across releases

The v0.1 release should document conventions before enforcing a package manager or runtime implementation.
