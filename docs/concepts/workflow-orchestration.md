# Workflow Orchestration

Workflow orchestration coordinates multi-step journeys.

Workflows may include state transitions, tool calls, policy checkpoints, human approvals, retry behavior, and compensation steps. The harness should integrate with existing workflow engines where useful instead of requiring a single runtime.
