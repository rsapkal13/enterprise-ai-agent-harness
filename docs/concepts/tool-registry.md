# Tool Registry

The Tool Registry defines safe wrappers around external actions and data access.

Tools may represent APIs, MCP tools, database queries, queue messages, workflow commands, or mock actions. Each tool should declare input and output expectations, target system, authentication mode, side-effect profile, risk, data classification, and audit requirements.
