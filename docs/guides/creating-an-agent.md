# Creating an Agent

An agent record defines identity and boundaries.

Include the agent's business identity, technical identity, supported channels, owners, allowed skills, and default context scopes. Avoid embedding provider-specific prompt details in the registry record unless they are portable metadata.
