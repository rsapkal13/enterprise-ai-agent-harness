# Quickstart

For v0.1, the project is a specification foundation.

1. Read the core model in `README.md`.
2. Review the architecture docs.
3. Inspect the JSON schemas in `schemas/`.
4. Explore the telco customer-care example.
5. Use the backlog to choose the next specification or runtime task.

No dependency installation is required for the initial documentation and schema work.
