# Observability Package

This package will model traces and evaluation results.

Audit has a separate package because audit records are governance evidence, while observability records are operational signals. The v0.1 files are placeholders for future contracts that connect agent actions to policy decisions, tool calls, workflow states, and business outcomes.
