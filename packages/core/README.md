# Core Package

The core package will hold shared TypeScript types and primitives used across the harness.

For v0.1, it only exposes placeholder contracts. Runtime behavior belongs in later releases after the schemas and architecture are stable.
