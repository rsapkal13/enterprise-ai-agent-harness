# Tool Gateway Package

The Tool Gateway will execute governed tool calls through adapters.

Adapters may wrap REST APIs, MCP tools, mocks, workflow commands, or data access. v0.1 only defines placeholder contracts.
