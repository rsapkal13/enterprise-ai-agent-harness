# Context Layer Package

This package will model context scopes and controlled context access.

For v0.1, it contains placeholder contracts only. Future releases should connect context scopes to policies, agents, skills, systems, and audit evidence.
