# Registries Package

This package will provide registry interfaces for skills, tools, and agents.

The v0.1 files describe package boundaries only. Future releases should add in-memory implementations, validation hooks, and persistence adapters.
