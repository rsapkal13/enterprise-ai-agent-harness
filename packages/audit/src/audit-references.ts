export interface AuditReferences {
  agentId?: string;
  skillId?: string;
  policyId?: string;
  toolId?: string;
  workflowId?: string;
  systemId?: string;
  evaluationId?: string;
}

// TODO: Align with schemas/audit-event.schema.json references.
